import { SessionAuthenticationService } from './SessionAuthenticationService';
import { Config } from '../objects/Config';

export class MessagingService {
	constructor(config: Config, auth: SessionAuthenticationService) {
	}
}